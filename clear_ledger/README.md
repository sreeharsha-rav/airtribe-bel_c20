# Clear Ledger

A simple ledger application built with Django. It allows users to link bank accounts, categorize and track transactions, get monthly budgets per category, get spending reports and manage their finances. The project demonstrates schema design, model relationships, queries, query sets, transactions and query optimization.